# 为自学坊贡献

感谢你帮助自学坊变得更准确、更完整。我们欢迎不同规模的贡献，但是否进入官方网站，最终以内容标准、自动检查和维护者审核结果为准。

## 从哪里开始

无需修改代码也可以贡献。请选择最合适的 Issue 表单：

- **推荐新资源**：推荐公开课、开放教材、论文或学习工具；
- **报告失效链接**：报告打不开、跳转错误或内容已经下线的链接；
- **修正内容错误**：指出课程描述、难度、前置要求或学科关系中的错误；
- **提议新增学科**：讨论新的学科、课程结构或跨学科关系。

较大的架构调整、新学科或设计改动，请先创建 Issue 并达成基本共识，再开始实现。

## Pull Request 流程

1. Fork 仓库，并从最新的 `main` 创建独立分支。
2. 每个 Pull Request 只解决一个明确问题。
3. 按 [内容收录标准](docs/CONTENT_STANDARDS.md) 核查新增或修改的资源。
4. 在本地运行 `npm run lint` 和 `npm run build`。
5. 填写 Pull Request 模板，并关联对应 Issue。
6. 等待自动检查和维护者审核；根据反馈继续修改。

请不要直接提交生成目录、依赖目录、编辑器缓存、密钥或个人数据。

## 内容修改要求

新增资源至少应提供：

- 准确的名称和原始链接；
- 提供者或学校；
- 资源类型；
- 难度和前置要求；
- 简短、客观、可验证的介绍；
- 最近验证日期；
- 推荐理由或可核查的评价依据。

禁止提交盗版教材、未经授权的网盘副本、来源不明的文件、推广链接以及无法验证的评价。

## 代码修改要求

- 保持现有视觉语言、移动端体验和无障碍能力；
- 不无故引入新的运行时依赖；
- 不在源码中写入 API 密钥、统计密钥之外的秘密或用户数据；
- 涉及路由、菜单或目录时，同时检查桌面端和移动端；
- 不改变无关文件，避免把多个主题混入同一个 Pull Request。

## 审核与合并

自动检查通过并不代表一定合并。维护者还会核对准确性、版权、资源质量、结构一致性和项目方向。维护者可以要求修改、暂缓收录或关闭不符合范围的提议，并会尽量说明原因。

合并通常使用 Squash merge，以保持提交历史简洁。请保持讨论友善、聚焦事实，并遵守 [社区行为准则](CODE_OF_CONDUCT.md)。

## 贡献授权

提交贡献即表示你有权提供这些内容，并同意：

- 代码贡献按 MIT License 发布；
- 原创文字、课程路径和元数据按 CC BY-SA 4.0 发布；
- 第三方资源只提交合法链接和必要的事实性介绍，不复制受版权保护的正文。
